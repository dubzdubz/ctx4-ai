### Supabase

# Get these from https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App URL (used for OAuth metadata)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Service Role Key (for server-side token validation)
# Get from: Supabase Dashboard → Project Settings → API → Service Role Key (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key


### GitHub

# GitHub repository for memory storage
# Create a new repo (can be private) and paste the HTTPS URL here
GITHUB_REPO=https://github.com/youruser/my-memories.git

# GitHub Personal Access Token
# Create a fine-grained PAT at: https://github.com/settings/tokens?type=beta
# Required permissions: "Contents: Read and Write" on your memory repo
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

### Vercel
# Vercel OIDC Token (for Vercel Sandbox auth)
# Set automatically via `vercel link && vercel env pull`
VERCEL_OIDC_TOKEN=your-vercel-oidc-token


### Authorization

# Optional: Comma-separated list of allowed Supabase user IDs
# If set, only these users can access ctx_bash and ctx_instructions
# Get your user ID from: Supabase Dashboard → Authentication → Users
# Example: ALLOWED_USER_IDS=uuid-1,uuid-2,uuid-3
# If not set, any authenticated user can access
ALLOWED_USER_IDS=
